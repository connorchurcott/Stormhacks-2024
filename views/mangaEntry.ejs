<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= manga ? (manga.title.romaji || manga.title.english) : 'Manga Details' %></title>
    <link rel="stylesheet" href="/css/mangaEntry.css">
</head>
<body>
    <%- include('partials/header') %>

    <main>
        <% if (manga) { %>
            <h1><%= manga.title.romaji || manga.title.english %></h1>
            <img src="<%= manga.coverImage.large %>" alt="Manga Cover">
            <p><%= manga.description %></p>
            <p><strong>Chapters:</strong> <%= manga.chapters %></p>
            <p><strong>Volumes:</strong> <%= manga.volumes %></p>
            <p><strong>Status:</strong> <%= manga.status %></p>
            <p><strong>Genres:</strong> <%= manga.genres.join(', ') %></p>
        <% } else { %>
            <p><%= error || 'No manga details available.' %></p>
        <% } %>
    </main>
</body>
</html>
